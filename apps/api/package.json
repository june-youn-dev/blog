{
    "name": "@blog/api",
    "private": true,
    "description": "Node-based tooling for the blog-api Cloudflare Worker crate.",
    "packageManager": "pnpm@10.33.2+sha512.a90faf6feeab71ad6c6e57f94e0fe1a12f5dcc22cd754db40ae9593eb6a3e0b6b12e3540218bb37ae083404b1f2ce6db2a4121e979829b4aff94b99f49da1cf8",
    "engines": {
        "node": ">=24"
    },
    "scripts": {
        "migrate:local": "wrangler d1 migrations apply blog --local",
        "sql:lint": "PIPX_HOME=.pipx PIPX_BIN_DIR=.pipx/bin PIPX_LOG_DIR=.pipx/logs pipx run sqlfluff==4.1.0 lint migrations",
        "sql:fix": "PIPX_HOME=.pipx PIPX_BIN_DIR=.pipx/bin PIPX_LOG_DIR=.pipx/logs pipx run sqlfluff==4.1.0 fix migrations",
        "rust:fmt": "cargo +nightly fmt",
        "rust:fmt:check": "cargo +nightly fmt -- --check",
        "rust:lint": "cargo clippy --target wasm32-unknown-unknown -- -D warnings",
        "rust:check": "cargo check --target wasm32-unknown-unknown",
        "rust:test": "cargo test",
        "rust:build": "cargo install -q worker-build && worker-build --release",
        "worker:dev": "wrangler dev",
        "bindings:format": "prettier --write \"../site/src/api-types/**/*.ts\"",
        "bindings:generate": "cargo test --lib export_bindings && pnpm run bindings:format",
        "bindings:check": "pnpm run bindings:generate && git diff --exit-code ../site/src/api-types",
        "check": "pnpm run rust:fmt:check && pnpm run rust:lint && pnpm run rust:check && pnpm run rust:test && pnpm run bindings:check && pnpm run sql:lint"
    },
    "devDependencies": {
        "prettier": "^3.8.2",
        "wrangler": "^4.81.1"
    },
    "pnpm": {
        "onlyBuiltDependencies": [
            "workerd",
            "esbuild"
        ]
    }
}
